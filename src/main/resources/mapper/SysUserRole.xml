<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sundata.edu.dao.SysUserRoleDao">
    
    <resultMap type="com.sundata.edu.domain.SysUserRole" id="BaseResultMap">
        <result property="userRoleId" column="user_role_id" />
        <result property="userId" column="user_id" />
        <result property="roleId" column="role_id" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <insert id="batchUserRole">
        insert into sys_user_role(user_id, role_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.userId},#{item.roleId})
        </foreach>
    </insert>

    <insert id="batchDelUserRole">
        delete from sys_user_role where 1=0 or
        <foreach item="item" index="index" collection="list" separator="or">
            (user_id=#{item.userId} and role_id=#{item.roleId})
        </foreach>
    </insert>
    <delete id="deleteAuthUser">
        delete from sys_user_role where user_id=#{userId} and role_id=#{roleId}
    </delete>

</mapper>